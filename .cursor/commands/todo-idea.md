/todo:idea <idea text>

Survey and design this idea, then add it to local backlog:

1. Survey relevant code/spec/evals and identify risks + dependencies.
2. Create an idea note in `tmp/proposals/ideas/<timestamp>-<slug>.md`.
3. Append a new item to `tmp/todos/active.md` including:
   - reason
   - expected benefit
   - candidate success/risk metrics
4. Update `tmp/todos/handoff.md` with immediate next task.
5. Keep one feature per commit and validate before commit if code changes.
